# Local generated credentials and tokens
client_secret.json
token.json

# Browser profile data
EdgeProfile/

# Python environment and caches
.venv/
__pycache__/
.pytest_cache/
.tmp_cache/

# Local SQLite state
*.sqlite3

# Premiere: generated overlay assets and local editing state
data/premiere_assets/
data/premiere_history.json
data/premiere_styles.json

# Editing: run artifacts, transcripts, plans, renders and delivered episodes.
# All of it is derived from the footage and can be rebuilt; the delivered
# videos in particular are hundreds of megabytes each.
data/editing/

# A third-party tool writes and holds this open; it is not part of the project.
gude-*.log
